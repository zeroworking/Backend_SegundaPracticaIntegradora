{{!-- <link rel="stylesheet" href="/css/profile.css">
<div class="container">
    <div>
        <h1>Tu perfil (sin datos sensibles)</h1>
        <p>{{user.name}}</p>
        <p>{{user.email}}</p>
        <p>{{user.age}}</p>
        <p>{{user.role}}</p>
    </div>
</div>
 --}}

<link rel="stylesheet" href="/css/profile.css">  
<div class="container">
  <div class="row justify-content-center mt-5">
    <div class="col-md-6">
        <h1>Tu perfil (sin datos sensibles)</h1><br>
        <p>Usuario: {{user.name}}<p>
        <p>Email: {{user.email}}</p>
        <p>Edad: {{user.age}}</p>
        <p>Rol: {{user.role}}</p>
        <p>Carrito: {{user.cart}}</p>
    </div>
  </div>
</div>


